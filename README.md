# solidjs-counter

Mini démo SolidJS : un compteur auto-incrémenté + une requête HTTP (todo) affichée à l’écran.

## Utilisation

Ouvrir `index.html` dans un navigateur moderne.

> Note : ce projet charge SolidJS et TanStack Query via CDN.
